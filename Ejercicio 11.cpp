#include<iostream>
using namespace std;

int main(){
	int n,a[100],p=0,q=0;
	cout<<"Cantidad de elementos: ";
	cin>>n;
	for(int i=0;i<n;i++){
		cout<<"Valor "<<i<<": ";
		cin>>a[i];
		if
		(a[i]>0)p+=a[i];
		else if
		(a[i]<0)q+=a[i];
	}
	
}
